language: bash

matrix:  
  include:      
    - name: "Windows - MSVC"
      os: windows      
      install: choco install --force cmake
      env: PATH="$PATH:/c/Program File/CMake/bin/"

before_install:
  - eval "${MATRIX_EVAL}"

before_script:
  - export
  - ls /c/Program\ File/
  - mkdir -p build
  - cd build  
  - cmake -DCMAKE_BUILD_TYPE=${BUILD_TYPE} ..

script:
  - cmake --build . -- -j10
  - ctest -j10 -C ${BUILD_TYPE} --output-on-failure

notifications:
  email: false

    
